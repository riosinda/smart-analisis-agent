[project]
name = "smart-analisis-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "pandas>=2.0.0",
    "plotly>=6.0.0",
    "seaborn>=0.13.2",
    "ipykernel>=6.0.0",
]

[tool.uv.workspace]
members = [
    "app",
    "frontend",
]
